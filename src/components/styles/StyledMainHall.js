import styled from "styled-components";

export const StyledMainHall = styled.div`
  background: #111;
  grid-template-rows: repeat(calc(80vw / 4));
  grid-template-columns: repeat(calc(80vw / 2));
`;
